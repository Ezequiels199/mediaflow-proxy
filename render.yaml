services:
  - type: web
    name: mediaflow-proxy
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: API_KEY
        value: miclave
      - key: SUPERCLAVE
        value: mipassword
