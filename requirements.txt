# Web framework
fastapi>=0.95.0,<1.0.0
uvicorn[standard]>=0.22.0

# HTTP clients / async streaming
httpx[http2]>=0.24.0
aiohttp>=3.8.5
aiofiles>=23.1.0

# HTML parsing / scraping
beautifulsoup4>=4.12.2
lxml>=4.9.3
regex>=2024.7.14

# HLS / m3u8 parsing
m3u8>=0.7.1

# multipart forms / uploads
python-multipart>=0.0.6

# caching (opcional pero útil para proxys)
diskcache>=5.4.0
cachetools>=5.3.0

# YAML + utilidades
PyYAML>=6.0
typing-extensions>=4.7.1

# Si usas encriptación/headers avanzados
cryptography>=40.0.2
